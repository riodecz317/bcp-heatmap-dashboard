# Local tooling / environment (never portable across machines)
.venv/
.claude/settings.local.json
.vscode/

# Windows folder metadata
desktop.ini

# Operational logs (local troubleshooting only, not project data)
bcp_publish.log
bcp_watcher.log
