# Local tooling / environment (never portable across machines)
.venv/
.claude/settings.local.json
.vscode/

# Windows folder metadata
desktop.ini

# bcp_publish.ps1's operational log (local troubleshooting only, not project data)
bcp_publish.log
